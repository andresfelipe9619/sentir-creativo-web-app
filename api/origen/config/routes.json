{
  "routes": [
    {
      "method": "GET",
      "path": "/origens",
      "handler": "origen.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/origens/count",
      "handler": "origen.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/origens/:id",
      "handler": "origen.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/origens",
      "handler": "origen.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/origens/:id",
      "handler": "origen.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/origens/:id",
      "handler": "origen.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
