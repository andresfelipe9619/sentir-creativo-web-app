{
  "routes": [
    {
      "method": "GET",
      "path": "/calendarios",
      "handler": "calendarios.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/calendarios/count",
      "handler": "calendarios.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/calendarios/:id",
      "handler": "calendarios.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/calendarios",
      "handler": "calendarios.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/calendarios/:id",
      "handler": "calendarios.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/calendarios/:id",
      "handler": "calendarios.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
