{
  "routes": [
    {
      "method": "GET",
      "path": "/atributos",
      "handler": "atributo.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/atributos/count",
      "handler": "atributo.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/atributos/:id",
      "handler": "atributo.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/atributos",
      "handler": "atributo.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/atributos/:id",
      "handler": "atributo.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/atributos/:id",
      "handler": "atributo.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
