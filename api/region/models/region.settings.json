{
  "kind": "collectionType",
  "collectionName": "regions",
  "info": {
    "name": "region"
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "nombre": {
      "type": "string"
    },
    "ciudades": {
      "collection": "ciudad",
      "via": "region"
    },
    "pais": {
      "model": "pais",
      "via": "regiones"
    }
  }
}
