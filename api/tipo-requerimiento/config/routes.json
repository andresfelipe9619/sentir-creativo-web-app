{
  "routes": [
    {
      "method": "GET",
      "path": "/tipo-requerimientos",
      "handler": "tipo-requerimiento.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/tipo-requerimientos/count",
      "handler": "tipo-requerimiento.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/tipo-requerimientos/:id",
      "handler": "tipo-requerimiento.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/tipo-requerimientos",
      "handler": "tipo-requerimiento.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/tipo-requerimientos/:id",
      "handler": "tipo-requerimiento.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/tipo-requerimientos/:id",
      "handler": "tipo-requerimiento.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
