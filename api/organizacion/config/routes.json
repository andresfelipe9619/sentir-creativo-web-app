{
  "routes": [
    {
      "method": "GET",
      "path": "/organizacions",
      "handler": "organizacion.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/organizacions/count",
      "handler": "organizacion.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/organizacions/:id",
      "handler": "organizacion.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/organizacions",
      "handler": "organizacion.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/organizacions/:id",
      "handler": "organizacion.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/organizacions/:id",
      "handler": "organizacion.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
