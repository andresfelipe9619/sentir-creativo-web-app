{
  "routes": [
    {
      "method": "GET",
      "path": "/pais",
      "handler": "pais.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/pais/count",
      "handler": "pais.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/pais/:id",
      "handler": "pais.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/pais",
      "handler": "pais.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/pais/:id",
      "handler": "pais.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/pais/:id",
      "handler": "pais.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
